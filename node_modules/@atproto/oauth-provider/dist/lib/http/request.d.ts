import type { IncomingMessage, ServerResponse } from 'node:http';
import { UrlReference } from './url.js';
export declare function validateHeaderValue(req: IncomingMessage, name: keyof IncomingMessage['headers'], allowedValues: readonly (string | null)[]): void;
export declare function validateFetchMode(req: IncomingMessage, res: ServerResponse, expectedMode: readonly (null | 'navigate' | 'same-origin' | 'no-cors' | 'cors')[]): void;
export declare function validateFetchDest(req: IncomingMessage, res: ServerResponse, expectedDest: readonly (null | 'document' | 'embed' | 'font' | 'image' | 'manifest' | 'media' | 'object' | 'report' | 'script' | 'serviceworker' | 'sharedworker' | 'style' | 'worker' | 'xslt')[]): void;
export declare function validateFetchSite(req: IncomingMessage, res: ServerResponse, expectedSite: readonly (null | 'same-origin' | 'same-site' | 'cross-site' | 'none')[]): void;
export declare function validateReferer(req: IncomingMessage, res: ServerResponse, reference: UrlReference, allowNull: true): URL | null;
export declare function validateReferer(req: IncomingMessage, res: ServerResponse, reference: UrlReference, allowNull?: false): URL;
export declare function setupCsrfToken(req: IncomingMessage, res: ServerResponse, cookieName?: string): Promise<void>;
export declare function validateSameOrigin(req: IncomingMessage, res: ServerResponse, origin: string, allowNull?: boolean): void;
export declare function validateCsrfToken(req: IncomingMessage, res: ServerResponse, csrfToken: unknown, cookieName?: string, clearCookie?: boolean): void;
export declare function parseHttpCookies(req: IncomingMessage): null | Record<string, undefined | string>;
export type ExtractRequestMetadataOptions = {
    /**
     * A function that determines whether a given IP address is trusted. The
     * function is called with the IP addresses and its index in the list of
     * forwarded addresses (starting from 0, 0 corresponding to the ip of the
     * incoming HTTP connection, and the last item being the first proxied IP
     * address in the proxy chain, deduced from the `X-Forwarded-For` header). The
     * function should return `true` if the IP address is trusted, and `false`
     * otherwise.
     *
     * @see {@link https://www.npmjs.com/package/proxy-addr} for a utility that
     * allows you to create a trust function.
     */
    trustProxy?: (addr: string, i: number) => boolean;
};
export type RequestMetadata = {
    userAgent?: string;
    ipAddress: string;
    port: number;
};
export declare function extractRequestMetadata(req: IncomingMessage, options?: ExtractRequestMetadataOptions): RequestMetadata;
export declare function extractLocales(req: IncomingMessage): string[];
export declare function negotiateResponseContent<T extends string>(req: IncomingMessage, types: readonly T[]): T | undefined;
//# sourceMappingURL=request.d.ts.map