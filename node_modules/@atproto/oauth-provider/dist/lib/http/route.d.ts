import type { IncomingMessage, ServerResponse } from 'node:http';
import { SubCtx } from './context.js';
import { MethodMatcherInput } from './method.js';
import { Params, Path } from './path.js';
import { Middleware } from './types.js';
export type RouteCtx<T, P extends Params> = SubCtx<T, {
    params: Readonly<P>;
}>;
export type RouteMiddleware<T, P extends Params, Req = IncomingMessage, Res = ServerResponse> = Middleware<RouteCtx<T, P>, Req, Res>;
/**
 * @example
 * ```ts
 * createRoute<{ foo: string }>('GET', '/foo/:foo', function (req, res) {
 *   console.log(this.params.foo) // OK
 *   console.log(this.params.bar) // Error
 * })
 *
 * createRoute<{ foo: string }>(['POST', 'PUT'], '/foo/:foo', function (req, res) {
 *   console.log(this.params.foo) // OK
 *   console.log(this.params.bar) // Error
 * })
 * ```
 */
export declare function createRoute<P extends Params, T = void, Req extends IncomingMessage = IncomingMessage, Res extends ServerResponse = ServerResponse>(method: MethodMatcherInput, path: Path<P>, ...mw: RouteMiddleware<T, P, Req, Res>[]): Middleware<T, Req, Res>;
//# sourceMappingURL=route.d.ts.map