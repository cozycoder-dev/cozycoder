import { OAuthAuthorizationRequestParameters, OAuthAuthorizationServerMetadata } from '@atproto/oauth-types';
import { Account } from '../account/account.js';
import { ClientAuth } from '../client/client-auth.js';
import { ClientId } from '../client/client-id.js';
import { Client } from '../client/client.js';
import { DeviceId } from '../device/device-id.js';
import { RequestMetadata } from '../lib/http/request.js';
import { OAuthHooks } from '../oauth-hooks.js';
import { Signer } from '../signer/signer.js';
import { Code } from './code.js';
import { RequestDataAuthorized } from './request-data.js';
import { RequestInfo } from './request-info.js';
import { RequestStore } from './request-store.js';
import { RequestUri } from './request-uri.js';
export declare class RequestManager {
    protected readonly store: RequestStore;
    protected readonly signer: Signer;
    protected readonly metadata: OAuthAuthorizationServerMetadata;
    protected readonly hooks: OAuthHooks;
    protected readonly tokenMaxAge: number;
    constructor(store: RequestStore, signer: Signer, metadata: OAuthAuthorizationServerMetadata, hooks: OAuthHooks, tokenMaxAge?: number);
    protected createTokenExpiry(): Date;
    createAuthorizationRequest(client: Client, clientAuth: ClientAuth, input: Readonly<OAuthAuthorizationRequestParameters>, deviceId: null | DeviceId, dpopJkt: null | string): Promise<RequestInfo>;
    protected create(client: Client, clientAuth: ClientAuth, parameters: Readonly<OAuthAuthorizationRequestParameters>, deviceId?: null | DeviceId): Promise<RequestInfo>;
    protected validate(client: Client, clientAuth: ClientAuth, parameters: Readonly<OAuthAuthorizationRequestParameters>, dpop_jkt: null | string): Promise<Readonly<OAuthAuthorizationRequestParameters>>;
    get(uri: RequestUri, deviceId: DeviceId, clientId?: ClientId): Promise<RequestInfo>;
    setAuthorized(uri: RequestUri, client: Client, account: Account, deviceId: DeviceId, deviceMetadata: RequestMetadata): Promise<Code>;
    /**
     * @note If this method throws an error, any token previously generated from
     * the same `code` **must** me revoked.
     */
    findCode(client: Client, clientAuth: ClientAuth, code: Code): Promise<RequestDataAuthorized>;
    delete(uri: RequestUri): Promise<void>;
}
//# sourceMappingURL=request-manager.d.ts.map